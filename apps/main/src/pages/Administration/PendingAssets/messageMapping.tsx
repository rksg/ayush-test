/* eslint-disable max-len */
import { defineMessage } from 'react-intl'

export const MessageMapping = {
  hide_devive_tooltip: defineMessage({ defaultMessage: 'Hidden devices will be hidden from the table and excluded from future refreshes, but will remain accessible under "Show hidden devices".' })
}