/* eslint-disable max-len */
import { defineMessage } from 'react-intl'

export const MessageMapping = {
  ignore_devive_tooltip: defineMessage({ defaultMessage: 'Ignored devices will be hidden from the table and excluded from future refreshes, but will remain accessible under "Show Ignored Devices".' })
}