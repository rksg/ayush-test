/* eslint-disable max-len */
import { defineMessage } from 'react-intl'

export const MessageMapping = {
  migrate_description: defineMessage({ defaultMessage: 'Ruckus ONE helps you migrate uploaded ZD configuration' }),
  migrate_tooltip: defineMessage({ defaultMessage: 'ZD configuration will be migrated to newly created venue. WLAN won\'t be migrated.' })
}
