import { Space as AntSpace } from 'antd'
import styled                from 'styled-components'

export const HostnameSpace = styled(AntSpace)`
  font-size: var(--acx-subtitle-4-font-size);
  margin-left: 8px;
`
