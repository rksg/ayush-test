ARG CI_SERVICE_BASE
FROM ${CI_SERVICE_BASE}node:master--latest as build-base

WORKDIR /app

RUN apk add curl

ARG ENV_FILE
COPY ${ENV_FILE} .
RUN source ${ENV_FILE} \
  && npm config set registry "http://artifactory.cicd.rks-cloud.com/artifactory/api/npm/npm/" \
  && curl -u "${ARTI_USER}:${ARTI_PASSWORD}" "http://artifactory.cicd.rks-cloud.com/artifactory/api/npm/auth" >> ~/.npmrc

# Install dependencies
COPY package.json package-lock.json ./
COPY patches ./patches
RUN npm install

COPY .eslintignore \
  .eslintrc.json \
  babel.config.json \
  jest.config.ts \
  jest.preset.ts \
  jest.setup.js \
  nx.json \
  webpack.config.base.js \
  tsconfig.base.json \
  workspace.json \
  ./

COPY .storybook ./.storybook
COPY apps ./apps
COPY libs ./libs
COPY tools/tests ./tools/tests

# dump git information
RUN source ${ENV_FILE} \
  && echo NX_GIT_SHA=$DRONE_COMMIT_SHA >> .env \
  && echo NX_GIT_DATE=\'$(date -R "@$((DRONE_BUILD_CREATED))")\' >> .env

###############################################################
FROM build-base as acx-ui-lint
RUN node ./node_modules/.bin/nx run-many --target=lint --all --verbose
RUN touch lint-done

# We should validate the tsc validator in the tsconfig of apps that wont be built yet
FROM build-base as acx-ui-tslint
RUN node ./node_modules/.bin/nx run ra:tsc
###############################################################
FROM node:14.21.1-alpine as build-locales
WORKDIR /app
RUN apk add curl
ARG ENV_FILE
COPY ${ENV_FILE} .
RUN source ${ENV_FILE} \
  && npm config set registry "http://artifactory.cicd.rks-cloud.com/artifactory/api/npm/npm/" \
  && curl -u "${ARTI_USER}:${ARTI_PASSWORD}" "http://artifactory.cicd.rks-cloud.com/artifactory/api/npm/auth" >> ~/.npmrc
COPY package.json ./
RUN npm install react-intl lodash
COPY apps ./apps
COPY libs ./libs
COPY tools/docker/locales ./tools/docker/locales
RUN chmod +x tools/docker/locales/generate.sh
RUN tools/docker/locales/generate.sh

FROM build-base as build
COPY --from=build-locales /app/apps/main/src/locales ./apps/main/src/locales
RUN node ./node_modules/.bin/nx run-many --target=build --projects=main

###############################################################
FROM build-base as acx-ui-base-test
ENV NODE_OPTIONS=--max-old-space-size=10240

# Force dependency with lint & build stages
COPY --from=acx-ui-lint   /app/lint-done   ./lint-done
COPY --from=build         /app/dist/apps   ./dist/apps

COPY cicd_tool ./cicd_tool
COPY test_cases.table ./test_cases.table
RUN chmod a+x ./cicd_tool/run_test_cases_tool.sh

FROM acx-ui-base-test as acx-ui-group1-test
ARG PRECOMMIT
ARG GIT_COMMIT_BRANCH
RUN if [ "true" ]; then \
	    ./cicd_tool/run_test_cases_tool.sh Group1; \
	else mkdir -p /app/coverage; fi

FROM acx-ui-base-test as acx-ui-group2-test
ARG PRECOMMIT
ARG GIT_COMMIT_BRANCH
RUN if [ "true" ]; then \
        ./cicd_tool/run_test_cases_tool.sh Group2; \
	else mkdir -p /app/coverage; fi

FROM acx-ui-base-test as acx-ui-group3-test
ARG PRECOMMIT=true
ARG GIT_COMMIT_BRANCH
RUN if [ "true" ]; then \
        ./cicd_tool/run_test_cases_tool.sh Group3; \
	else mkdir -p /app/coverage; fi

FROM acx-ui-base-test as acx-ui-group4-test
ARG PRECOMMIT=true
ARG GIT_COMMIT_BRANCH
RUN if [ "true" ]; then \
        ./cicd_tool/run_test_cases_tool.sh Group4; \
	else mkdir -p /app/coverage; fi

FROM acx-ui-base-test as acx-ui-group5-test
ARG PRECOMMIT
ARG GIT_COMMIT_BRANCH
RUN if [ "true" ]; then \
        ./cicd_tool/run_test_cases_tool.sh Group5; \
	else mkdir -p /app/coverage; fi

FROM build-base as acx-ui-sonar-run
COPY --from=acx-ui-group1-test /app/coverage /app/coverage
COPY --from=acx-ui-group2-test /app/coverage /app/coverage
COPY --from=acx-ui-group3-test /app/coverage /app/coverage
COPY --from=acx-ui-group4-test /app/coverage /app/coverage
COPY --from=acx-ui-group5-test /app/coverage /app/coverage
WORKDIR /app
RUN touch tests-done

###############################################################
FROM nginx:1.21.6-alpine

WORKDIR /usr/share/nginx/html

# Remove default nginx static assets
RUN rm -rf ./*

COPY tools/docker/nginx/conf.d/* /etc/nginx/conf.d/
COPY tools/docker/nginx/nginx.conf \
  tools/docker/nginx/start-nginx.sh \
  tools/docker/nginx/env.json.template \
  /etc/nginx/

COPY --from=build /app/dist/apps ./
# Multistage: trigger run
COPY --from=acx-ui-sonar-run /app/tests-done /tmp/

RUN \
  # Create structure to match deployment need
  mkdir -p tenant/t && \
  # Move all files into it
  mv -v main/* tenant/t/. && \
  # Remove the main folder
  rm -rf main

EXPOSE 8080

CMD ["/bin/sh", "/etc/nginx/start-nginx.sh"]
