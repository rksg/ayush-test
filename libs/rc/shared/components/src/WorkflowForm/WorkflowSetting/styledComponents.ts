import styled from 'styled-components/macro'


export const EmptyButton = styled.div`
  color: var(--acx-accents-blue-50);
  width: auto;
  height: 105px;
  display: flex;
  justify-content: center;
  align-items: center;
  cursor: pointer;

  border: 1px dotted var(--acx-accents-blue-50);
  border-radius: 5px;
`
