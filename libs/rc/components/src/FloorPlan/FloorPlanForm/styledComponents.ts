import { Form } from 'antd'
import styled   from 'styled-components/macro'

export const FloorNumberFormItem = styled(Form.Item)`
  .ant-form-item-extra {
    padding: 0;
    position: absolute;
    right: 0;
    top: -7px;
  }
`
