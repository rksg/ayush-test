import styled from 'styled-components/macro'

export const SimpleListUl = styled.ul`
  list-style-type: none;
  padding-inline-start: 0 !important;
  padding: 4px;
  li {
    padding: 2px 0;
  }
  h4 {
    color: var(--acx-primary-white);
    font-weight: 800;
  }
`
