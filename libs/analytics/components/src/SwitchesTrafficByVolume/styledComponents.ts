import styled from 'styled-components/macro'

export const SelectPort = styled.div`
  margin-bottom: 5px;

  .ant-select {
    width: 120px;
    position: absolute;
    top: -30px;
    right: 0px;
  }
`