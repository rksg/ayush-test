import { Row } from 'antd'
import styled  from 'styled-components/macro'

export const CascaderFilterWrapper = styled.div`padding-bottom: 15px;`
export const GridRow = styled(Row).attrs({ gutter: 12 })``
